# Bug triage + fix

Investigate failing test(s) for `module.or.path` given this failure output: <paste short trace>. Explain root cause in one sentence, propose the smallest code change to fix it, then show the patch and a one-line rationale.

Required output:
- Root cause (one sentence).
- Minimal patch (unified diff or updated file content).
- One-line regression risk note and suggested test to add.

Placeholders:
- `<short trace>` — trimmed failure log
- `module.or.path` — file or module under test
