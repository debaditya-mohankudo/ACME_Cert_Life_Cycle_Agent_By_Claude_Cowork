# Code change (small patch)

You are my pair programmer. Edit `path/to/file` to implement: <one-sentence goal>. Keep style consistent with the repo and preserve public APIs. Make minimal changes and avoid unrelated refactors.

Required output:
- A unified diff or the updated file contents only.
- One-line test plan (what to run / which tests to update).

Placeholders:
- `path/to/file` — target file path
- `<one-sentence goal>` — goal summary

Constraints:
- Prefer the smallest, highest-confidence change.
- Mention any assumptions made.
