# Add unit test

Add a unit test for `path/to/file::function` covering the edge case: <brief case>. Use existing test frameworks and fixtures. Provide the new test file content and required imports.

Required output:
- New test file content (pytest style).
- Any helper or fixture changes (if strictly necessary) with explanation.

Placeholders:
- `<brief case>` — short description of the edge case
- `path/to/file::function` — fully qualified target
