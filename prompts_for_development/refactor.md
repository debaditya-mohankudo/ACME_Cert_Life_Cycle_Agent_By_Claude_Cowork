# Refactor (safe, local)

Refactor `function_or_class` in `path/to/file` to improve readability without changing behavior or public API. Keep tests passing. Return only the updated file contents and a 1–2 sentence summary of changes.

Required output:
- Updated file content (single file).
- Short summary of refactor and why it's safer/readable.

Constraints:
- Do not change external behavior or public signatures.
- Avoid introducing new external dependencies.
