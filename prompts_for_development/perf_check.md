# Performance / complexity check

Analyze complexity and bottlenecks of `function` in `path/to/file`. List hotspots (bulleted), one optimization suggestion per hotspot, and potential regressions to watch for.

Required output:
- Complexity summary (big-O)
- Hotspots and one-line optimizations
- Risks and regression notes (bulleted)

Placeholders:
- `function` — target function
- `path/to/file` — source file path
