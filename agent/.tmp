Plan: Standardize logging imports

Steps:
1. Find all project files that `import logging`.
2. For each file that only creates a module-level logger via `logging.getLogger(...)`, replace that with `from logger import logger` and remove `import logging`.
3. For files that call `logging.exception()` / `logging.warning()` / etc., replace those calls to `logger.exception()` / `logger.warning()` and import `logger`.
4. Leave `main.py`'s `import logging` alone (used for structlog/basicConfig).
5. Run tests.

Files to change (candidate list):
- agent/nodes/*.py (nodes that currently import logging)
- agent/nodes/finalizer.py
- agent/nodes/order.py
- agent/nodes/planner.py
- acme/dns_challenge.py
- mcp_server.py (also replace `logging.exception` calls)
- tests/test_ca_detection.py (remove/replace unused logging import)

I will now perform the edit pass.
